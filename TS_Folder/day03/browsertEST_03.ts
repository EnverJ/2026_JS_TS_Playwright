import { BROWSERS, ENVS } from "./enum_03";


let browserName = "chrome";
switch(browserName){
    case BROWSERS.CHROME:
        console.log("launch chrome");
        break;
    default:
        break;
}
//launch chrome